ln -sf /usr/share/zoneinfo/australia/perth && hwclock --systohc && pacman -S vim && vim /etc/locale.conf && vim /etc/locale.gen && locale-gen && echo "#this is the hostname file" > /etc/hostname && vim /etc/hostname && mkinitcpio -P && useradd -m schneiderman && passwd schneiderman && usermod -aG wheel,video,audio,optical,storage schneiderman && pacman -S sudo grub networkmanager && EDITOR=vim visudo && systemctl enable NetworkManager && grub-install /dev/vda && grub-mkconfig -o /boot/grub/grub.cfg && exit
