ln -sf /usr/share/zoneinfo/australia/perth /etc/localtime && hwclock --systohc && pacman -S vim && vim /etc/locale.conf && vim /etc/locale.gen && locale-gen && echo "yourhostnamehere" > /etc/hostname && vim /etc/hostname && mkinitcpio -P && useradd -m schneiderman && passwd schneiderman && usermod -aG wheel,video,audio,optical,storage schneiderman && pacman -S sudo grub networkmanager gnome x-server wayland xorg sddm  && EDITOR=vim visudo && systemctl enable NetworkManager && systemctl enable sddm && grub-install /dev/vda && grub-mkconfig -o /boot/grub/grub.cfg && exit
